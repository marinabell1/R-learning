###############################################################
#
#That’s the basic interface for executing R code in RStudio. 
#   Think you have it? If so, try  doing these simple tasks. 
#If you execute everything correctly, you should end up with
#the same number that you started with:
#  1. Choose any number and add 2 to it.
#  2. Multiply the result by 3.
#  3. Subtract 6 from the answer.
#  4. Divide what you get by 3.

################################################################

7 + 2
#[1]*3
9*3
27 - 6
21/3

#succeded :)

################################################################


die <- 1:6
die - 1

#functions are mechanism for processing thing in an optimal way
#there are functions already built, but we can also built our own
#the data out in the function is it's argument
#function sample helps us roll our die

sample(x = die, size=1 )
sample(x = die, size=1 )















